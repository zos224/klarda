export const metadata = {
    title: "Đăng nhập người dùng | Klarda",
    description: "Đăng nhập người dùng Klarda",
};

const LoginLayout = ({children}) => {
    return (
        <>
           {children} 
        </>
    );
}

export default LoginLayout;