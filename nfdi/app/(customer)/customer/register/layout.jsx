export const metadata = {
    title: "Đăng ký người dùng | Klarda",
    description: "Đăng ký người dùng Klarda",
};

const RegisterLayout = ({children}) => {
    return (
        <>
           {children} 
        </>
    );
}

export default RegisterLayout;