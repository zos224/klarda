export const metadata = {
    title: "Tài khoản | Klarda",
    description: "Trang tài khoản cá nhân"
}
const AccountLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default AccountLayout