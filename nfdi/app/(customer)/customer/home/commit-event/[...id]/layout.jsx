export const metadata = {
    title: "Đăng tải sự kiện | Klarda",
    description: "Đăng tải sự kiện"
}
const CURestaurantLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default CURestaurantLayout