export const metadata = {
    title: "Tạo | Cập nhật trường hợp sử dụng",
}
const CUPPLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default CUPPLayout