export const metadata = {
    title: "Tạo - Cập nhật dự án | Klarda",
}
const CUPPLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default CUPPLayout