export const metadata = {
    title: "Thêm | Cập nhật sự kiện",
}
const CURestaurantLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default CURestaurantLayout