export const dynamic = 'force-dynamic'
export const metadata = {
    title: "Signin | Admin Klarda",
  };

const SignInLayout = ({children}) => {
return (
        <div>
        {children}
        </div>
    );
};

export default SignInLayout;