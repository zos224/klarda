export const metadata = {
    title: "Tạo - Cập nhật công cụ | Klarda",
}
const CURestaurantLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default CURestaurantLayout